import * as React from 'react';
import { UnknownResult } from '../../common/types';
import { IResolvedQueriesProps } from './ResolvedQueries';
export interface IResolvedQueryProps extends Omit<IResolvedQueriesProps, 'resultsWithErrorTitles'> {
    result: UnknownResult;
    errorTitle: string;
}
export declare const ResolvedQuery: React.FunctionComponent<IResolvedQueryProps>;
